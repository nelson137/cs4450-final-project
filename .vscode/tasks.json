{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "group": "build",
            "type": "process",
            "command": "${workspaceFolder}/build.sh",
            "args": [],
        }
    ]
}
